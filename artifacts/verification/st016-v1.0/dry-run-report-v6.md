# ST-016 External Dry Run Report — Run 6 (Final Acceptance Audit)

## Repository
- **Repository:** valentinlineiro/takt-theory
- **Branch:** `main` (fresh, isolated clone)
- **Commit SHA:** `9eb503570db31908440e05a08f7a74492377c642`

## Environment
- **OS:** Linux 6.18.5, x86_64 (container)
- **Node.js:** v22.22.2
- **npm:** 10.9.7
- **Lean:** not installed (see below)

## Executed exactly as instructed
`./scripts/bootstrap.sh && ./scripts/verify.sh` — `npm install` succeeded (81 packages). Halted at `bootstrap.sh`'s Lean-provisioning step, exit code 1. `verify.sh` never reached (script correctly aborts under `set -euo pipefail` when `bootstrap.sh` fails).

## Lean installation failure: (A) or (B)?

**(B) — external execution-environment restriction.** `elan` downloaded successfully from `raw.githubusercontent.com`; the toolchain fetch from `release.lean-lang.org` failed: `CONNECT tunnel failed, response 403`. Proxy diagnostics confirm an explicit policy denial for `release.lean-lang.org:443` in this sandbox, while `registry.npmjs.org` (used successfully by `npm install`) is allowlisted. Identical cause and evidence as Runs 2–5.

## Checklist results

| Item | Result |
|---|---|
| `bootstrap.sh` behaves as documented | **PASS** |
| `verify.sh` executes correctly as far as the environment allows | **PASS** (never reached, per (B); no script defect) |
| Lean installation failure classification | **(B)** |
| Runtime test suite passes | **PASS** — 283/283 tests, 76/76 files |
| EXP-004 passes | **PASS** — 3/3 tests, 3/3 files |
| Verification artifacts exist only in `artifacts/verification/st016-v1.0/` | **PASS** — sweep of `artifacts/verification/` found only that directory's `environment.json`, `hashes.json`, `st016-v1.0-report.md`, plus the static `dry-run-template.md`; no duplicates elsewhere |
| `hashes.json` matches every referenced artifact | **PASS** — all 7 entries (`theory_manifest`, `st016_spec`, `lean_sufficiency`, `lean_witness`, `ts_temporal_ablation`, `ts_uncertainty_ablation`, `ts_contract_ablation`) recomputed independently and matched exactly |
| `theory-manifest.yml` internally consistent | **PASS** — all 14 referenced paths exist; capability IDs match `CONFORMANCE.md` |
| No stale or duplicate verification artifacts | **PASS** |
| No absolute local `file:///` links anywhere in the repository | **PASS** — `git grep "file:///home"` and `git grep "file:///"` across the entire tracked repository both return zero matches. This resolves the 46-file gap identified in Run 5. |

## Observation (not scored against the checklist)

`npm install` (inside `bootstrap.sh`) mutated `package-lock.json` in this clone — a plain-`npm`-version artifact: npm 10.9.7 strips a `"libc"` metadata field from several optional-dependency entries that the committed lockfile carries (generated by a newer npm on Node v24). No package versions, resolutions, or the dependency tree itself changed. This does not affect any of the checklist items above (it is not part of `hashes.json`, and the test suite result is unaffected) and is noted only for completeness.

## Overall Result

**PASS WITH ENVIRONMENTAL LIMITATION**

Every checklist item resolves cleanly on a fresh, isolated clone of `9eb5035`: the artifact-location, hash-integrity, `theory-manifest.yml` consistency, no-duplicates, and repository-wide absolute-link checks all pass without qualification. No reproducible defect attributable to the repository remains. The only unresolved item — completing the Lean 4 build and Lean-side witness certification — is blocked exclusively by this audit sandbox's network policy (classification B), not by anything in the repository.
